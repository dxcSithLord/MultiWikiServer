node_modules
node_modules_off
scratch
localhost.*
*.key
/dist
TiddlyWiki5
*.tgz
/.vscode/settings.json
editions/mws-docs/tiddlers/$__StoryList.tid
/tests
/tests-output
/tests-temp

# Local Claude Code settings (machine/user-specific, not portable across checkouts).
# Shared skills/agents under .claude/ are committed; only local overrides are ignored.
.claude/settings.local.json
.claude/**/*.local.json